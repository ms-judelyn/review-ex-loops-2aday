#include <iostream>

int main() {
    // Loop to iterate from 0 to 5 (inclusive)
    for (int i = 0; i <= 5; i++) {
        std::cout << i << std::endl; // Print the current value of 'i' followed by a newline
    }
    return 0; // Return 0 to indicate successful execution
}
