#include <iostream>
using namespace std;

int main() {
    // Initialize a variable i to 10
    int i = 10;

    // While i >= 1, print i and decrement i by 1
    while (i >= 1) {
        cout << i << endl;
        i--;
    }

    return 0;
}
